# 📘 PROJETO_DANIEL
## 🧩 Tema escolhido para o novo CRUD

O tema escolhido foi o CRUD de Funcionários, complementando o CRUD já existente de Clientes.
O sistema permite cadastrar, listar, alterar e excluir funcionários, integrando-se ao banco de dados MySQL.

## 🚀 Resumo dos desafios implementados

### Implementação de um novo CRUD (Create, Read, Update, Delete) completo para a entidade Funcionários.

### Criação de formulários dinâmicos para inserção e alteração de dados.

### Integração com o banco de dados via MySQL e PHP (MySQLi).

### Criação e ajuste de arquivos modulares (config.inc.php, menu.php, topo.php, rodape.php) para reutilização de código.

### Correção de erros de sintaxe e tratamento de requisições POST para evitar warnings e melhorar a segurança.

### Utilização de estruturas de diretórios organizadas, separando as funcionalidades administrativas na pasta /admin.

## ⚙️ Instruções rápidas para rodar o projeto

### Instalar o XAMPP (ou outro servidor local que rode PHP e MySQL).

### Copiar a pasta PROJETO_DANIEL para o diretório:
```makefile
C:\xampp\htdocs\
```


### Abrir o phpMyAdmin e importar o arquivo:
```bash
db.sql
```

### Iniciar o Apache e o MySQL no painel do XAMPP.

### Acessar o sistema no navegador:

```bash
http://localhost/PROJETO_DANIEL/index.php
```

### Para acessar a área administrativa:

```bash
http://localhost/PROJETO_DANIEL/admin/admin.php
```

## 📦 Resumo da Entrega

### Arquivos do Projeto: Todos os arquivos PHP e diretórios compactados em .zip.

### Banco de Dados: Script de criação e estrutura disponível no arquivo db.sql.

### Arquivo de documentação: Este README.md.
